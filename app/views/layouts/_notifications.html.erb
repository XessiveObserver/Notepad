<div class="mx-auto container text-center">
      <% if notice.present? %>
        <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg inline-block" id="notice">
          <%= notice %>
        </p>
      <% end %>
      <% if notice.present? %>
        <p class="py-2 px-3 bg-green-50 mb-5 text-orange-500 font-medium rounded-lg inline-block" id="notice">
          <%= alert %>
        </p>
      <% end %>      
</div>