
<h1><%= @note.title %></h1>

<p><%= @note.body %></p>

<%= link_to "Edit Note", edit_note_path(@note) %>
<%= link_to "Delete Note", note_path(@note), data: {
    turbo_method: :delete,
    turbo_confirm: "Are you sure?"
}%>